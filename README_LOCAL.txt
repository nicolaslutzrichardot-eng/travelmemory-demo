SwissTravelMemory Web Local V1.8

Cette version est conçue pour être servie par l'application SwissTravelMemory Mac sur:
http://127.0.0.1:8765/

Elle récupère automatiquement les voyages détectés par PhotoKit via:
GET /api/trips

Ne publiez pas cette variante seule sur GitHub pour le pont local : elle attend l'API locale du Mac.
